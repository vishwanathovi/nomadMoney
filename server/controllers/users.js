const User = require("../models/Users");

module.exports = {
	createUser: (req, res) => {
		// Check if username or Email already present
		// Save the user and send success message
	},
	loginUser: (req, res) => {},
	isUser: (req, res) => {},
	getUser: (req, res) => {}
};
